export default class CodeBuilder {
    constructor(comps) {
        this.comps = comps;
    }
    gen() {
        for (let i = 0; i < this.comps.length; i++) {
            const comp = this.comps[i];
        }
    }
}
